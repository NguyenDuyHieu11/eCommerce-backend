'use strict'

class CartController{

    // add product to cart
    addToCart = async (req, res, next) => {
        try {
            const { userId, product } = req.body
            
        }
    }
}