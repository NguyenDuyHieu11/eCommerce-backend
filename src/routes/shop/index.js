'use strict'

const express = require('express');
const router = express.Router();

// TODO: Add shop management routes
// These would be authenticated shop operations (update profile, get shop details, etc.)
// Example:
// router.get('/profile', shopController.getProfile);
// router.patch('/profile', shopController.updateProfile);
// router.delete('/profile', shopController.deleteShop);
// router.get('/stats', shopController.getStats);

module.exports = router;



